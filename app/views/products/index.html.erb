<p id="notice"><%= notice %></p>

<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <%if !current_user.products.map(&:id).include?(product.id)%>
        <%if !current_user.purchased_products.map(&:product_id).include?(product.id)%>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.formatted_price %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
    <!--<td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
        <td><%= link_to 'Buy', "/products/#{product.id}/buy" %></td>
      </tr>
        <%end%>
      <%end%>
    <% end %>
  </tbody>
</table>

<br>

<div class="new_product">
<%= link_to 'New Product', new_product_path %>
</div>

<div class="photos">
  <img class= "apple_watch" src="/assets/applewatch.png" alt="apple watch">
  <img class= ""src="" alt="">
</div>
